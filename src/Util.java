public class Util {
    public enum RescourseType {
        Worker, Machine, Equipment, SeniorWorker, SeniorMachine, SeniorEquipment
    }
    public enum WorkSpace{
        Assemble,Technology
    }
    public enum Production_Mode{
        SP,BP
    };
}
